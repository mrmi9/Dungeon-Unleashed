# Audio Placeholders

This directory is reserved for first-pass audio assets.

The current prototype uses procedural placeholder sounds from `scripts/audio/AudioFeedback.gd` so the playable build has immediate feedback without committing final `.wav` or `.ogg` assets yet.
